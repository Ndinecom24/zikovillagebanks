<div>
    <!-- Page Header -->
    <div class="content-header">
        <div class="container-fluid">
            <div class="z-page-header">
                <div class="d-flex align-items-center justify-content-between flex-wrap">
                    <div>
                        <h1>
                            @if ($bank->logo)
                                <img src="{{ asset('storage/' . $bank->logo) }}" alt="" style="width:36px;height:36px;border-radius:8px;object-fit:cover;margin-right:0.5rem;">
                            @else
                                <i class="fas fa-university mr-2" style="color: var(--z-gold)"></i>
                            @endif
                            {{ $bank->name }}
                        </h1>
                        <p><code>{{ $bank->code }}</code> &middot; {{ ucfirst($bank->status) }} &middot; Created {{ $bank->created_at->format('d M Y') }}</p>
                    </div>
                    <div class="d-flex" style="gap:0.5rem;">
                        <a href="{{ route('village-banks.create', ['edit' => $bank->id]) }}" class="btn btn-light" style="border-radius:8px;">
                            <i class="fas fa-edit mr-1"></i> Edit
                        </a>
                        <a href="{{ route('village-banks.index') }}" class="btn btn-light" style="border-radius:8px;">
                            <i class="fas fa-arrow-left mr-1"></i> Back
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <section class="content">
        <div class="container-fluid">

            @if (session()->has('message'))
                <div class="alert alert-success" style="border-radius:10px;font-size:0.9rem;"><i class="fas fa-check-circle mr-1"></i> {{ session('message') }}</div>
            @endif

            {{-- Tabs --}}
            <ul class="nav nav-pills mb-3" style="gap:0.5rem;">
                @foreach (['overview' => 'Overview', 'members' => 'Members', 'circles' => 'Circles', 'finance' => 'Financials'] as $key => $label)
                    <li class="nav-item">
                        <a href="#" wire:click.prevent="$set('activeTab', '{{ $key }}')"
                           class="nav-link {{ $activeTab === $key ? 'active' : '' }}"
                           style="border-radius:8px;font-size:0.88rem;font-weight:600;{{ $activeTab === $key ? 'background:var(--z-green);border-color:var(--z-green);' : '' }}">
                            {{ $label }}
                        </a>
                    </li>
                @endforeach
            </ul>

            {{-- ═══════════════ OVERVIEW TAB ═══════════════ --}}
            @if ($activeTab === 'overview')
                @php $s = $this->stats; @endphp
                <div class="row mb-3">
                    <div class="col-md-3">
                        <div class="card z-card" style="border-left:4px solid #2563eb;">
                            <div class="card-body py-3">
                                <small class="text-muted">Total Circles</small>
                                <h3 class="mb-0 font-weight-bold">{{ $s['totalCircles'] }}</h3>
                                <small style="color:#16a34a;">{{ $s['activeCircles'] }} active</small>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="card z-card" style="border-left:4px solid #7c3aed;">
                            <div class="card-body py-3">
                                <small class="text-muted">Total Members</small>
                                <h3 class="mb-0 font-weight-bold">{{ $s['totalMembers'] }}</h3>
                                <small style="color:#f59e0b;">{{ $s['adminCount'] }} admins</small>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="card z-card" style="border-left:4px solid #0891b2;">
                            <div class="card-body py-3">
                                <small class="text-muted">Total Contributions</small>
                                <h3 class="mb-0 font-weight-bold" style="color:#0891b2;">K{{ number_format($s['totalContributions'], 2) }}</h3>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="card z-card" style="border-left:4px solid var(--z-gold);">
                            <div class="card-body py-3">
                                <small class="text-muted">Distributed</small>
                                <h3 class="mb-0 font-weight-bold" style="color:var(--z-gold);">K{{ number_format($s['totalDistributed'], 2) }}</h3>
                                <small class="text-muted">{{ $s['totalShareouts'] }} shareouts</small>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="row mb-3">
                    <div class="col-md-3">
                        <div class="card z-card" style="border-left:4px solid #ea580c;">
                            <div class="card-body py-3">
                                <small class="text-muted">Total Loans</small>
                                <h3 class="mb-0 font-weight-bold">{{ $s['totalLoans'] }}</h3>
                                <small style="color:#ea580c;">K{{ number_format($s['totalLoanAmount'], 2) }}</small>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="card z-card" style="border-left:4px solid #16a34a;">
                            <div class="card-body py-3">
                                <small class="text-muted">Total Repaid</small>
                                <h3 class="mb-0 font-weight-bold" style="color:#16a34a;">K{{ number_format($s['totalRepaid'], 2) }}</h3>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="card z-card" style="border-left:4px solid #dc2626;">
                            <div class="card-body py-3">
                                <small class="text-muted">Outstanding</small>
                                <h3 class="mb-0 font-weight-bold" style="color:#dc2626;">K{{ number_format($s['totalOutstanding'], 2) }}</h3>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="card z-card" style="border-left:4px solid #7c3aed;">
                            <div class="card-body py-3">
                                <small class="text-muted">Penalties</small>
                                <h3 class="mb-0 font-weight-bold" style="color:#7c3aed;">K{{ number_format($s['totalPenalties'], 2) }}</h3>
                            </div>
                        </div>
                    </div>
                </div>

                {{-- Bank Info Card --}}
                <div class="card z-card">
                    <div class="card-header"><h3><i class="fas fa-id-card mr-2" style="color:var(--z-green)"></i>Bank Information</h3></div>
                    <div class="card-body">
                        <div class="row" style="font-size:0.9rem;">
                            <div class="col-md-6">
                                <table class="table table-borderless table-sm mb-0">
                                    <tr><td class="text-muted" style="width:130px;">Name</td><td><strong>{{ $bank->name }}</strong></td></tr>
                                    <tr><td class="text-muted">Code</td><td><code>{{ $bank->code }}</code></td></tr>
                                    <tr><td class="text-muted">Status</td><td>
                                        @php $sc = $bank->status === 'active' ? ['#f0fdf4','#166534','#bbf7d0'] : ['#f3f4f6','#374151','#e5e7eb']; @endphp
                                        <span class="badge" style="background:{{ $sc[0] }};color:{{ $sc[1] }};border:1px solid {{ $sc[2] }};font-size:0.8rem;font-weight:600;">{{ ucfirst($bank->status) }}</span>
                                    </td></tr>
                                    <tr><td class="text-muted">Created By</td><td>{{ $bank->creator->name ?? '--' }}</td></tr>
                                </table>
                            </div>
                            <div class="col-md-6">
                                <table class="table table-borderless table-sm mb-0">
                                    <tr><td class="text-muted" style="width:130px;">Email</td><td>{{ $bank->email ?? '--' }}</td></tr>
                                    <tr><td class="text-muted">Phone</td><td>{{ $bank->phone ?? '--' }}</td></tr>
                                    <tr><td class="text-muted">Address</td><td>{{ $bank->address ?? '--' }}</td></tr>
                                    <tr><td class="text-muted">Description</td><td>{{ $bank->description ?? '--' }}</td></tr>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            @endif

            {{-- ═══════════════ MEMBERS TAB ═══════════════ --}}
            @if ($activeTab === 'members')
                <div class="card z-card">
                    <div class="card-header d-flex align-items-center justify-content-between">
                        <h3><i class="fas fa-users mr-2" style="color:var(--z-green)"></i>Members ({{ $members->total() }})</h3>
                        <button wire:click="openAddMember" class="btn-zesco">
                            <i class="fas fa-user-plus mr-1"></i> Add Member
                        </button>
                    </div>
                    <div class="card-body p-0">
                        <div class="table-responsive">
                            <table class="table table-hover z-table mb-0">
                                <thead>
                                    <tr><th>Member</th><th>Email</th><th>Role</th><th>Joined</th><th style="width:140px;">Actions</th></tr>
                                </thead>
                                <tbody>
                                    @forelse ($members as $m)
                                        <tr>
                                            <td>
                                                <div class="d-flex align-items-center" style="gap:0.5rem;">
                                                    <div class="z-avatar-sm z-avatar-initials">{{ strtoupper(substr($m->name, 0, 1)) }}</div>
                                                    <strong>{{ $m->name }}</strong>
                                                </div>
                                            </td>
                                            <td style="font-size:0.85rem;">{{ $m->email }}</td>
                                            <td>
                                                @php
                                                    $rc = $m->pivot->role === 'admin'
                                                        ? ['#eff6ff','#1e40af','#bfdbfe']
                                                        : ['#f3f4f6','#374151','#e5e7eb'];
                                                @endphp
                                                <span class="badge" style="background:{{ $rc[0] }};color:{{ $rc[1] }};border:1px solid {{ $rc[2] }};font-size:0.78rem;font-weight:600;">{{ ucfirst($m->pivot->role) }}</span>
                                            </td>
                                            <td style="font-size:0.82rem;color:#6b7280;">{{ $m->pivot->joined_at ? \Carbon\Carbon::parse($m->pivot->joined_at)->format('d M Y') : '--' }}</td>
                                            <td>
                                                <div class="d-flex" style="gap:0.4rem;">
                                                    @if ($m->pivot->role === 'member')
                                                        <button wire:click="changeRole({{ $m->id }}, 'admin')" class="btn btn-sm btn-outline-primary" style="border-radius:6px;font-size:0.75rem;" title="Promote to Admin">
                                                            <i class="fas fa-arrow-up"></i>
                                                        </button>
                                                    @else
                                                        <button wire:click="changeRole({{ $m->id }}, 'member')" class="btn btn-sm btn-outline-secondary" style="border-radius:6px;font-size:0.75rem;" title="Demote to Member">
                                                            <i class="fas fa-arrow-down"></i>
                                                        </button>
                                                    @endif
                                                    <button wire:click="confirmRemoveMember({{ $m->id }})" class="btn btn-sm btn-outline-danger" style="border-radius:6px;font-size:0.75rem;" title="Remove">
                                                        <i class="fas fa-user-minus"></i>
                                                    </button>
                                                </div>
                                            </td>
                                        </tr>
                                    @empty
                                        <tr><td colspan="5" class="text-center py-4" style="color:#94a3b8;"><i class="fas fa-users fa-2x mb-2 d-block"></i>No members yet.</td></tr>
                                    @endforelse
                                </tbody>
                            </table>
                        </div>
                    </div>
                    @if ($members->hasPages())
                        <div class="card-footer bg-white border-top">{{ $members->links() }}</div>
                    @endif
                </div>
            @endif

            {{-- ═══════════════ CIRCLES TAB ═══════════════ --}}
            @if ($activeTab === 'circles')
                <div class="card z-card">
                    <div class="card-header">
                        <h3><i class="fas fa-circle-notch mr-2" style="color:var(--z-green)"></i>Circles ({{ $circles->count() }})</h3>
                    </div>
                    <div class="card-body p-0">
                        <div class="table-responsive">
                            <table class="table table-hover z-table mb-0">
                                <thead>
                                    <tr><th>Circle</th><th>Members</th><th>Duration</th><th>Start</th><th>Status</th><th>Created By</th></tr>
                                </thead>
                                <tbody>
                                    @forelse ($circles as $c)
                                        @php
                                            $sc = ['draft'=>['#fffbeb','#92400e','#fde68a'],'active'=>['#f0fdf4','#166534','#bbf7d0'],'completed'=>['#f3f4f6','#374151','#e5e7eb']][$c->status] ?? ['#f3f4f6','#374151','#e5e7eb'];
                                        @endphp
                                        <tr>
                                            <td><strong>{{ $c->name }}</strong></td>
                                            <td>{{ $c->members_count }}</td>
                                            <td>{{ $c->duration_months }} months</td>
                                            <td style="font-size:0.85rem;">{{ $c->start_date->format('d M Y') }}</td>
                                            <td><span class="badge" style="background:{{ $sc[0] }};color:{{ $sc[1] }};border:1px solid {{ $sc[2] }};font-size:0.78rem;font-weight:600;">{{ ucfirst($c->status) }}</span></td>
                                            <td style="font-size:0.85rem;">{{ $c->creator->name ?? '--' }}</td>
                                        </tr>
                                    @empty
                                        <tr><td colspan="6" class="text-center py-4" style="color:#94a3b8;"><i class="fas fa-circle-notch fa-2x mb-2 d-block"></i>No circles yet.</td></tr>
                                    @endforelse
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            @endif

            {{-- ═══════════════ FINANCE TAB ═══════════════ --}}
            @if ($activeTab === 'finance')
                @php $s = $this->stats; @endphp
                <div class="row">
                    <div class="col-lg-6">
                        <div class="card z-card">
                            <div class="card-header"><h3><i class="fas fa-chart-pie mr-2" style="color:var(--z-gold)"></i>Financial Summary</h3></div>
                            <div class="card-body" style="font-size:0.9rem;">
                                <ul class="list-unstyled mb-0" style="line-height:2.4;">
                                    <li class="d-flex justify-content-between border-bottom pb-1"><span class="text-muted">Total Contributions</span><strong style="color:#0891b2;">K{{ number_format($s['totalContributions'], 2) }}</strong></li>
                                    <li class="d-flex justify-content-between border-bottom pb-1"><span class="text-muted">Total Loans Issued</span><strong style="color:#ea580c;">K{{ number_format($s['totalLoanAmount'], 2) }}</strong></li>
                                    <li class="d-flex justify-content-between border-bottom pb-1"><span class="text-muted">Total Repaid</span><strong style="color:#16a34a;">K{{ number_format($s['totalRepaid'], 2) }}</strong></li>
                                    <li class="d-flex justify-content-between border-bottom pb-1"><span class="text-muted">Outstanding Balance</span><strong style="color:#dc2626;">K{{ number_format($s['totalOutstanding'], 2) }}</strong></li>
                                    <li class="d-flex justify-content-between border-bottom pb-1"><span class="text-muted">Penalties Collected</span><strong style="color:#7c3aed;">K{{ number_format($s['totalPenalties'], 2) }}</strong></li>
                                    <li class="d-flex justify-content-between"><span class="text-muted">Distributed (Shareouts)</span><strong style="color:var(--z-gold);">K{{ number_format($s['totalDistributed'], 2) }}</strong></li>
                                </ul>
                            </div>
                        </div>
                    </div>
                    <div class="col-lg-6">
                        <div class="card z-card">
                            <div class="card-header"><h3><i class="fas fa-tachometer-alt mr-2" style="color:var(--z-green)"></i>Key Metrics</h3></div>
                            <div class="card-body">
                                @php
                                    $repayRate = $s['totalLoanAmount'] > 0 ? round(($s['totalRepaid'] / $s['totalLoanAmount']) * 100) : 0;
                                    $interestEarned = max(0, $s['totalRepaid'] - $s['totalLoanAmount'] + $s['totalOutstanding']);
                                @endphp
                                <div class="mb-3">
                                    <div class="d-flex justify-content-between mb-1" style="font-size:0.88rem;"><span class="text-muted">Loan Repayment Rate</span><strong>{{ $repayRate }}%</strong></div>
                                    <div class="progress" style="height:8px;border-radius:8px;">
                                        <div class="progress-bar" style="width:{{ $repayRate }}%;background:var(--z-green);border-radius:8px;"></div>
                                    </div>
                                </div>
                                <div class="row text-center mt-4" style="font-size:0.88rem;">
                                    <div class="col-4">
                                        <div class="p-2" style="background:#eff6ff;border-radius:10px;">
                                            <div style="font-weight:700;color:#2563eb;font-size:1.1rem;">{{ $s['totalCircles'] }}</div>
                                            <small class="text-muted">Circles</small>
                                        </div>
                                    </div>
                                    <div class="col-4">
                                        <div class="p-2" style="background:#f0fdf4;border-radius:10px;">
                                            <div style="font-weight:700;color:#16a34a;font-size:1.1rem;">{{ $s['totalLoans'] }}</div>
                                            <small class="text-muted">Loans</small>
                                        </div>
                                    </div>
                                    <div class="col-4">
                                        <div class="p-2" style="background:#fffbeb;border-radius:10px;">
                                            <div style="font-weight:700;color:var(--z-gold);font-size:1.1rem;">{{ $s['totalShareouts'] }}</div>
                                            <small class="text-muted">Shareouts</small>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            @endif

        </div>
    </section>

    {{-- Add Member Modal --}}
    @if ($showAddMember)
        <div class="modal fade show z-modal" style="display:block;background:rgba(0,0,0,0.5);" tabindex="-1">
            <div class="modal-dialog modal-dialog-centered" style="max-width:500px;">
                <div class="modal-content">
                    <div class="modal-header-zesco d-flex align-items-center justify-content-between">
                        <h5><i class="fas fa-user-plus mr-2"></i> Add Member to {{ $bank->name }}</h5>
                        <button type="button" class="close" wire:click="closeAddMember"><span>&times;</span></button>
                    </div>
                    <div class="modal-body">
                        <div class="mb-3">
                            <label class="z-label">Search User</label>
                            <input type="text" wire:model.debounce.300ms="memberSearch" class="form-control z-input" placeholder="Type name, email or staff number...">
                        </div>

                        @if ($this->searchUsers->count())
                            <div class="list-group mb-3" style="max-height:200px;overflow-y:auto;">
                                @foreach ($this->searchUsers as $u)
                                    <button type="button" wire:click="$set('selectedUserId', {{ $u->id }})"
                                            class="list-group-item list-group-item-action d-flex align-items-center justify-content-between {{ $selectedUserId == $u->id ? 'active' : '' }}"
                                            style="border:none;padding:0.5rem 0.75rem;">
                                        <div class="d-flex align-items-center" style="gap:0.5rem;">
                                            <div class="z-avatar-sm z-avatar-initials">{{ strtoupper(substr($u->name, 0, 1)) }}</div>
                                            <div>
                                                <strong style="font-size:0.88rem;">{{ $u->name }}</strong>
                                                <small class="d-block {{ $selectedUserId == $u->id ? 'text-white-50' : 'text-muted' }}">{{ $u->email }}</small>
                                            </div>
                                        </div>
                                        @if ($selectedUserId == $u->id)
                                            <i class="fas fa-check-circle"></i>
                                        @endif
                                    </button>
                                @endforeach
                            </div>
                        @elseif (strlen($memberSearch) >= 2)
                            <p class="text-muted text-center py-2" style="font-size:0.85rem;">No users found.</p>
                        @endif

                        <div class="mb-3">
                            <label class="z-label">Role</label>
                            <select wire:model.defer="memberRole" class="form-control z-input">
                                <option value="member">Member</option>
                                <option value="admin">Admin</option>
                            </select>
                        </div>
                        @error('selectedUserId') <small class="text-danger">{{ $message }}</small> @enderror
                    </div>
                    <div class="modal-footer">
                        <button wire:click="closeAddMember" class="btn btn-secondary" style="border-radius:8px;">Cancel</button>
                        <button wire:click="addMember" class="btn-zesco-green" wire:loading.attr="disabled" wire:target="addMember" {{ !$selectedUserId ? 'disabled' : '' }}>
                            <span wire:loading.remove wire:target="addMember"><i class="fas fa-user-plus mr-1"></i> Add Member</span>
                            <span wire:loading wire:target="addMember"><i class="fas fa-spinner fa-spin mr-1"></i> Adding...</span>
                        </button>
                    </div>
                </div>
            </div>
        </div>
    @endif

    {{-- Remove Member Modal --}}
    @if ($removeMemberId)
        <div class="modal fade show z-modal" style="display:block;background:rgba(0,0,0,0.5);" tabindex="-1">
            <div class="modal-dialog modal-dialog-centered" style="max-width:440px;">
                <div class="modal-content">
                    <div class="modal-header-zesco d-flex align-items-center justify-content-between">
                        <h5><i class="fas fa-user-minus mr-2"></i> Remove Member</h5>
                        <button type="button" class="close" wire:click="$set('removeMemberId', null)"><span>&times;</span></button>
                    </div>
                    <div class="modal-body text-center py-4">
                        <p>Remove <strong>{{ $removeMemberName }}</strong> from <strong>{{ $bank->name }}</strong>?</p>
                    </div>
                    <div class="modal-footer d-flex justify-content-between">
                        <button wire:click="$set('removeMemberId', null)" class="btn btn-secondary" style="border-radius:8px;">Cancel</button>
                        <button wire:click="removeMember" class="btn btn-danger" style="border-radius:8px;font-weight:600;">
                            <i class="fas fa-user-minus mr-1"></i> Remove
                        </button>
                    </div>
                </div>
            </div>
        </div>
    @endif
</div>

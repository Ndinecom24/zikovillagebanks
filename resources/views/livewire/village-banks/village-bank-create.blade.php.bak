<div>
    <!-- Page Header -->
    <div class="content-header">
        <div class="container-fluid">
            <div class="z-page-header">
                <div class="d-flex align-items-center justify-content-between flex-wrap">
                    <div>
                        <h1><i class="fas fa-university mr-2" style="color: var(--z-gold)"></i>{{ $editId ? 'Edit' : 'Create' }} Village Bank</h1>
                        <p>{{ $editId ? 'Update village bank details' : 'Register a new village banking organisation' }}</p>
                    </div>
                    <a href="{{ route('village-banks.index') }}" class="btn btn-light" style="border-radius:8px;">
                        <i class="fas fa-arrow-left mr-1"></i> Back to List
                    </a>
                </div>
            </div>
        </div>
    </div>

    <section class="content">
        <div class="container-fluid">

            @if ($successMessage)
                <div class="alert alert-success" style="border-radius:10px;font-size:0.9rem;">
                    <i class="fas fa-check-circle mr-1"></i> {{ $successMessage }}
                </div>
            @endif

            <div class="row">
                <div class="col-lg-8">
                    <div class="card z-card">
                        <div class="card-header">
                            <h3><i class="fas fa-building mr-2" style="color:var(--z-green)"></i>Bank Details</h3>
                        </div>
                        <form wire:submit.prevent="save">
                            <div class="card-body">
                                @if ($errors->any())
                                    <div class="alert alert-danger" style="border-radius:8px;font-size:0.85rem;padding:0.5rem 0.75rem;">
                                        <i class="fas fa-exclamation-circle mr-1"></i> Please fix the errors below.
                                    </div>
                                @endif

                                {{-- Name & Code --}}
                                <div class="row mb-3">
                                    <div class="col-md-8">
                                        <label class="z-label">Bank Name <span class="text-danger">*</span></label>
                                        <input type="text" wire:model.defer="name" class="form-control z-input" placeholder="e.g. Kafue Road Village Bank">
                                        @error('name') <small class="text-danger">{{ $message }}</small> @enderror
                                    </div>
                                    <div class="col-md-4">
                                        <label class="z-label">Code <span class="text-danger">*</span></label>
                                        <input type="text" wire:model.defer="code" class="form-control z-input" placeholder="e.g. KRVB" maxlength="20" style="text-transform:uppercase;">
                                        @error('code') <small class="text-danger">{{ $message }}</small> @enderror
                                    </div>
                                </div>

                                {{-- Description --}}
                                <div class="mb-3">
                                    <label class="z-label">Description</label>
                                    <textarea wire:model.defer="description" class="form-control z-input" rows="3" placeholder="Brief description of the village bank..."></textarea>
                                    @error('description') <small class="text-danger">{{ $message }}</small> @enderror
                                </div>

                                {{-- Contact --}}
                                <div class="row mb-3">
                                    <div class="col-md-6">
                                        <label class="z-label">Email</label>
                                        <input type="email" wire:model.defer="email" class="form-control z-input" placeholder="bank@example.com">
                                        @error('email') <small class="text-danger">{{ $message }}</small> @enderror
                                    </div>
                                    <div class="col-md-6">
                                        <label class="z-label">Phone</label>
                                        <input type="text" wire:model.defer="phone" class="form-control z-input" placeholder="+260 97X XXX XXX">
                                        @error('phone') <small class="text-danger">{{ $message }}</small> @enderror
                                    </div>
                                </div>

                                {{-- Address --}}
                                <div class="mb-3">
                                    <label class="z-label">Address</label>
                                    <input type="text" wire:model.defer="address" class="form-control z-input" placeholder="Physical address">
                                    @error('address') <small class="text-danger">{{ $message }}</small> @enderror
                                </div>

                                {{-- Logo --}}
                                <div class="mb-3">
                                    <label class="z-label">Logo <small class="text-muted">(optional, max 2 MB)</small></label>
                                    <input type="file" wire:model="logo" class="form-control z-input" accept="image/*">
                                    @error('logo') <small class="text-danger">{{ $message }}</small> @enderror
                                    <div wire:loading wire:target="logo" class="mt-1">
                                        <small class="text-muted"><i class="fas fa-spinner fa-spin mr-1"></i> Uploading...</small>
                                    </div>
                                </div>
                            </div>

                            <div class="card-footer bg-white border-top d-flex justify-content-end" style="gap:0.75rem;">
                                @if (!$editId)
                                    <button type="button" wire:click="resetForm" class="btn btn-secondary" style="border-radius:8px;">
                                        <i class="fas fa-redo mr-1"></i> Reset
                                    </button>
                                @endif
                                <button type="submit" class="btn-zesco-green" wire:loading.attr="disabled" wire:target="save">
                                    <span wire:loading.remove wire:target="save"><i class="fas fa-save mr-1"></i> {{ $editId ? 'Update' : 'Create' }} Village Bank</span>
                                    <span wire:loading wire:target="save"><i class="fas fa-spinner fa-spin mr-1"></i> Saving...</span>
                                </button>
                            </div>
                        </form>
                    </div>
                </div>

                <div class="col-lg-4">
                    <div class="card z-card">
                        <div class="card-header">
                            <h3><i class="fas fa-info-circle mr-2" style="color:var(--z-gold)"></i>About Village Banks</h3>
                        </div>
                        <div class="card-body" style="font-size:0.88rem;color:#4b5563;">
                            <ul class="list-unstyled mb-0" style="line-height:2;">
                                <li><i class="fas fa-check text-success mr-2"></i> Each village bank is an independent organisation</li>
                                <li><i class="fas fa-check text-success mr-2"></i> Banks have their own circles, members & rules</li>
                                <li><i class="fas fa-check text-success mr-2"></i> Members can belong to multiple banks</li>
                                <li><i class="fas fa-check text-success mr-2"></i> Admins manage their bank's operations</li>
                                <li><i class="fas fa-check text-success mr-2"></i> Each bank has separate financial reports</li>
                                <li><i class="fas fa-check text-success mr-2"></i> Code must be unique (used as identifier)</li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
</div>

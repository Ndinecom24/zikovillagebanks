<div>
    <!-- Page Header -->
    <div class="content-header">
        <div class="container-fluid">
            <div class="z-page-header">
                <div class="d-flex align-items-center justify-content-between flex-wrap">
                    <div>
                        <h1><i class="fas fa-coins mr-2" style="color: var(--z-gold)"></i>Shareout Calculator</h1>
                        <p>Calculate and distribute the end-of-cycle pool to members</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <section class="content">
        <div class="container-fluid">

            @if ($successMessage)
                <div class="alert alert-success" style="border-radius:10px;font-size:0.9rem;">
                    <i class="fas fa-check-circle mr-1"></i> {{ $successMessage }}
                </div>
            @endif
            @if (session()->has('warning'))
                <div class="alert alert-warning" style="border-radius:10px;font-size:0.9rem;">
                    <i class="fas fa-exclamation-triangle mr-1"></i> {{ session('warning') }}
                </div>
            @endif

            <div class="row">
                {{-- Left: Calculator --}}
                <div class="col-lg-8">
                    {{-- Circle select --}}
                    <div class="card z-card">
                        <div class="card-header">
                            <h3><i class="fas fa-calculator mr-2" style="color:var(--z-green)"></i>Calculate Shareout</h3>
                        </div>
                        <div class="card-body">
                            <div class="row mb-3">
                                <div class="col-md-4">
                                    <label class="z-label">Village Bank</label>
                                    <select wire:model="villageBankId" class="form-control z-input">
                                        <option value="">All Village Banks</option>
                                        @foreach ($this->villageBanks as $vb)
                                            <option value="{{ $vb->id }}">{{ $vb->name }} ({{ $vb->code }})</option>
                                        @endforeach
                                    </select>
                                </div>
                                <div class="col-md-4">
                                    <label class="z-label">Circle <span class="text-danger">*</span></label>
                                    <select wire:model="circleId" class="form-control z-input">
                                        <option value="">-- Select Circle --</option>
                                        @foreach ($this->circles as $c)
                                            <option value="{{ $c->id }}">{{ $c->name }} ({{ $c->members_count }} members)</option>
                                        @endforeach
                                    </select>
                                    @error('circleId') <small class="text-danger">{{ $message }}</small> @enderror
                                </div>
                                <div class="col-md-4 d-flex align-items-end">
                                    @if ($existingShareout)
                                        <div class="alert alert-info mb-0 w-100" style="border-radius:8px;font-size:0.85rem;padding:0.5rem 0.75rem;">
                                            <i class="fas fa-info-circle mr-1"></i> A shareout already exists for this circle (K{{ number_format($existingShareout->total_pool, 2) }}).
                                        </div>
                                    @elseif ($circleId)
                                        <button wire:click="preview" class="btn-zesco" wire:loading.attr="disabled" wire:target="preview">
                                            <span wire:loading.remove wire:target="preview"><i class="fas fa-search-dollar mr-1"></i> Preview Shareout</span>
                                            <span wire:loading wire:target="preview"><i class="fas fa-spinner fa-spin mr-1"></i> Calculating...</span>
                                        </button>
                                    @endif
                                </div>
                            </div>
                        </div>
                    </div>

                    {{-- Preview results --}}
                    @if ($previewed)
                        {{-- Pool breakdown --}}
                        <div class="row mb-3">
                            <div class="col-md-3">
                                <div class="card z-card" style="border-left:4px solid #2563eb;">
                                    <div class="card-body py-3 d-flex align-items-center justify-content-between">
                                        <div><small class="text-muted">Contributions</small><h4 class="mb-0 font-weight-bold">K{{ number_format($totalContributions, 2) }}</h4></div>
                                        <div style="width:40px;height:40px;border-radius:50%;background:#eff6ff;display:flex;align-items:center;justify-content:center;">
                                            <i class="fas fa-piggy-bank" style="font-size:1rem;color:#2563eb;"></i>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-3">
                                <div class="card z-card" style="border-left:4px solid #16a34a;">
                                    <div class="card-body py-3 d-flex align-items-center justify-content-between">
                                        <div><small class="text-muted">Interest Earned</small><h4 class="mb-0 font-weight-bold">K{{ number_format($totalInterest, 2) }}</h4></div>
                                        <div style="width:40px;height:40px;border-radius:50%;background:#f0fdf4;display:flex;align-items:center;justify-content:center;">
                                            <i class="fas fa-percentage" style="font-size:1rem;color:#16a34a;"></i>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-3">
                                <div class="card z-card" style="border-left:4px solid #dc2626;">
                                    <div class="card-body py-3 d-flex align-items-center justify-content-between">
                                        <div><small class="text-muted">Penalties</small><h4 class="mb-0 font-weight-bold">K{{ number_format($totalPenalties, 2) }}</h4></div>
                                        <div style="width:40px;height:40px;border-radius:50%;background:#fef2f2;display:flex;align-items:center;justify-content:center;">
                                            <i class="fas fa-gavel" style="font-size:1rem;color:#dc2626;"></i>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-3">
                                <div class="card z-card" style="border-left:4px solid var(--z-gold);">
                                    <div class="card-body py-3 d-flex align-items-center justify-content-between">
                                        <div><small class="text-muted">Total Pool</small><h4 class="mb-0 font-weight-bold" style="color:var(--z-gold);">K{{ number_format($totalPool, 2) }}</h4></div>
                                        <div style="width:40px;height:40px;border-radius:50%;background:#fffbeb;display:flex;align-items:center;justify-content:center;">
                                            <i class="fas fa-coins" style="font-size:1rem;color:var(--z-gold);"></i>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        {{-- Allocations table --}}
                        <div class="card z-card">
                            <div class="card-header d-flex align-items-center justify-content-between">
                                <h3><i class="fas fa-users mr-2" style="color:var(--z-green)"></i>Member Allocations ({{ count($allocations) }})</h3>
                                @if (!$existingShareout)
                                    <button wire:click="finalise" class="btn-zesco-green" wire:loading.attr="disabled" wire:target="finalise"
                                            onclick="return confirm('Are you sure you want to finalise this shareout? This action cannot be undone.')">
                                        <span wire:loading.remove wire:target="finalise"><i class="fas fa-check-double mr-1"></i> Finalise Shareout</span>
                                        <span wire:loading wire:target="finalise"><i class="fas fa-spinner fa-spin mr-1"></i> Finalising...</span>
                                    </button>
                                @endif
                            </div>
                            <div class="card-body p-0">
                                <div class="table-responsive">
                                    <table class="table table-hover z-table mb-0">
                                        <thead>
                                            <tr>
                                                <th>#</th>
                                                <th>Member</th>
                                                <th>Contributions</th>
                                                <th>Share %</th>
                                                <th>Profit Share</th>
                                                <th>Payout</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            @forelse ($allocations as $i => $alloc)
                                                <tr>
                                                    <td style="color:#94a3b8;font-size:0.82rem;">{{ $i + 1 }}</td>
                                                    <td>
                                                        <div class="d-flex align-items-center" style="gap:0.5rem;">
                                                            <div class="z-avatar-sm z-avatar-initials">{{ strtoupper(substr($alloc['name'], 0, 1)) }}</div>
                                                            <div>
                                                                <strong>{{ $alloc['name'] }}</strong>
                                                                <small class="d-block text-muted">{{ $alloc['email'] }}</small>
                                                            </div>
                                                        </div>
                                                    </td>
                                                    <td>K{{ number_format($alloc['contribution_total'], 2) }}</td>
                                                    <td>
                                                        <div class="d-flex align-items-center" style="gap:0.4rem;">
                                                            <div class="progress flex-grow-1" style="height:5px;border-radius:5px;max-width:60px;">
                                                                <div class="progress-bar" style="width:{{ $alloc['ratio'] }}%;background:var(--z-green);border-radius:5px;"></div>
                                                            </div>
                                                            <small style="font-size:0.8rem;">{{ $alloc['ratio'] }}%</small>
                                                        </div>
                                                    </td>
                                                    <td style="color:#16a34a;font-weight:600;">K{{ number_format($alloc['profit_share'], 2) }}</td>
                                                    <td style="font-weight:700;color:#1e40af;">K{{ number_format($alloc['payout_amount'], 2) }}</td>
                                                </tr>
                                            @empty
                                                <tr><td colspan="6" class="text-center py-4" style="color:#94a3b8;"><i class="fas fa-users fa-2x mb-2 d-block"></i>No members found.</td></tr>
                                            @endforelse
                                        </tbody>
                                        @if (count($allocations))
                                            <tfoot style="background:#f8fafc;font-weight:700;">
                                                <tr>
                                                    <td colspan="2" class="text-right">TOTALS</td>
                                                    <td>K{{ number_format(array_sum(array_column($allocations, 'contribution_total')), 2) }}</td>
                                                    <td>100%</td>
                                                    <td style="color:#16a34a;">K{{ number_format(array_sum(array_column($allocations, 'profit_share')), 2) }}</td>
                                                    <td style="color:#1e40af;">K{{ number_format(array_sum(array_column($allocations, 'payout_amount')), 2) }}</td>
                                                </tr>
                                            </tfoot>
                                        @endif
                                    </table>
                                </div>
                            </div>
                        </div>
                    @endif
                </div>

                {{-- Right: Past shareouts + info --}}
                <div class="col-lg-4">
                    <div class="card z-card">
                        <div class="card-header">
                            <h3><i class="fas fa-info-circle mr-2" style="color:var(--z-gold)"></i>How Shareout Works</h3>
                        </div>
                        <div class="card-body" style="font-size:0.88rem;color:#4b5563;">
                            <ul class="list-unstyled mb-0" style="line-height:2;">
                                <li><i class="fas fa-check text-success mr-2"></i> Select a circle to calculate</li>
                                <li><i class="fas fa-check text-success mr-2"></i> Contributions from all months are totalled</li>
                                <li><i class="fas fa-check text-success mr-2"></i> Interest = total repayments &minus; total loans</li>
                                <li><i class="fas fa-check text-success mr-2"></i> Penalties collected are added to the pool</li>
                                <li><i class="fas fa-check text-success mr-2"></i> Pool is split proportional to each member's share</li>
                                <li><i class="fas fa-check text-success mr-2"></i> Once finalised, the circle is marked <strong>Completed</strong></li>
                            </ul>
                        </div>
                    </div>

                    {{-- Past shareouts --}}
                    <div class="card z-card">
                        <div class="card-header">
                            <h3><i class="fas fa-history mr-2" style="color:var(--z-green)"></i>Past Shareouts</h3>
                        </div>
                        <div class="card-body p-0">
                            @if ($this->shareouts->count())
                                <div class="list-group list-group-flush">
                                    @foreach ($this->shareouts as $so)
                                        <button wire:click="viewShareout({{ $so->id }})" class="list-group-item list-group-item-action d-flex align-items-center justify-content-between" style="border:none;padding:0.75rem 1rem;">
                                            <div>
                                                <strong style="font-size:0.9rem;">{{ $so->circle->name ?? '--' }}</strong>
                                                <small class="d-block text-muted">{{ $so->created_at->format('d M Y') }} &middot; {{ $so->allocations->count() }} members</small>
                                            </div>
                                            <span style="font-weight:700;color:#1e40af;font-size:0.9rem;">K{{ number_format($so->total_pool, 2) }}</span>
                                        </button>
                                    @endforeach
                                </div>
                            @else
                                <div class="text-center py-4" style="color:#94a3b8;font-size:0.88rem;">
                                    <i class="fas fa-coins fa-2x mb-2 d-block"></i>
                                    No shareouts recorded yet.
                                </div>
                            @endif
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    {{-- Detail Modal --}}
    @if ($detailShareout)
        <div class="modal fade show z-modal" style="display:block;background:rgba(0,0,0,0.5);" tabindex="-1">
            <div class="modal-dialog modal-lg modal-dialog-centered" style="max-width:700px;">
                <div class="modal-content">
                    <div class="modal-header-zesco d-flex align-items-center justify-content-between">
                        <h5><i class="fas fa-coins mr-2"></i> Shareout &mdash; {{ $detailShareout->circle->name ?? '--' }}</h5>
                        <button type="button" class="close" wire:click="closeShareout"><span>&times;</span></button>
                    </div>
                    <div class="modal-body">
                        {{-- Pool breakdown --}}
                        <div class="row mb-3" style="font-size:0.88rem;">
                            <div class="col-3 text-center">
                                <small class="text-muted d-block">Contributions</small>
                                <strong>K{{ number_format($detailShareout->total_contributions, 2) }}</strong>
                            </div>
                            <div class="col-3 text-center">
                                <small class="text-muted d-block">Interest</small>
                                <strong style="color:#16a34a;">K{{ number_format($detailShareout->total_interest, 2) }}</strong>
                            </div>
                            <div class="col-3 text-center">
                                <small class="text-muted d-block">Penalties</small>
                                <strong style="color:#dc2626;">K{{ number_format($detailShareout->total_penalties, 2) }}</strong>
                            </div>
                            <div class="col-3 text-center">
                                <small class="text-muted d-block">Total Pool</small>
                                <strong style="color:var(--z-gold);">K{{ number_format($detailShareout->total_pool, 2) }}</strong>
                            </div>
                        </div>

                        <hr>

                        <h6 class="font-weight-bold mb-2"><i class="fas fa-users mr-1" style="color:var(--z-green)"></i> Allocations</h6>
                        <div class="table-responsive">
                            <table class="table table-sm z-table mb-0">
                                <thead>
                                    <tr><th>#</th><th>Member</th><th>Contributed</th><th>Profit</th><th>Payout</th></tr>
                                </thead>
                                <tbody>
                                    @foreach ($detailShareout->allocations->sortByDesc('payout_amount') as $alloc)
                                        <tr>
                                            <td style="color:#94a3b8;">{{ $loop->iteration }}</td>
                                            <td><strong>{{ $alloc->user->name ?? '--' }}</strong></td>
                                            <td>K{{ number_format($alloc->contribution_total, 2) }}</td>
                                            <td style="color:#16a34a;font-weight:600;">K{{ number_format($alloc->profit_share, 2) }}</td>
                                            <td style="color:#1e40af;font-weight:700;">K{{ number_format($alloc->payout_amount, 2) }}</td>
                                        </tr>
                                    @endforeach
                                </tbody>
                            </table>
                        </div>

                        <div class="mt-2 text-right" style="font-size:0.82rem;color:#6b7280;">
                            Finalised on {{ $detailShareout->created_at->format('d M Y, H:i') }}
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button wire:click="closeShareout" class="btn btn-secondary" style="border-radius:8px;"><i class="fas fa-times mr-1"></i> Close</button>
                    </div>
                </div>
            </div>
        </div>
    @endif
</div>

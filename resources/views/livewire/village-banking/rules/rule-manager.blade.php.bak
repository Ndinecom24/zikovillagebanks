<div>
    <!-- Page Header -->
    <div class="content-header">
        <div class="container-fluid">
            <div class="z-page-header">
                <div class="d-flex align-items-center justify-content-between flex-wrap">
                    <div>
                        <h1><i class="fas fa-book mr-2" style="color: var(--z-gold)"></i>Rules & Bylaws</h1>
                        <p>Manage village bank rules, policies and member acknowledgements</p>
                    </div>
                    <button wire:click="openCreate" class="btn-zesco">
                        <i class="fas fa-plus-circle"></i> Create Rule
                    </button>
                </div>
            </div>
        </div>
    </div>

    <section class="content">
        <div class="container-fluid">

            {{-- Flash --}}
            @if (session()->has('message'))
                <div class="alert alert-success" style="border-radius: 10px; font-size: 0.9rem;">
                    <i class="fas fa-check-circle mr-1"></i> {{ session('message') }}
                </div>
            @endif

            {{-- Stat Cards --}}
            <div class="row mb-3">
                <div class="col-md-3">
                    <div class="card z-card" style="border-left: 4px solid var(--z-green);">
                        <div class="card-body py-3 d-flex align-items-center justify-content-between">
                            <div>
                                <small class="text-muted">Total Rules</small>
                                <h3 class="mb-0 font-weight-bold">{{ $totalRules }}</h3>
                            </div>
                            <div style="width:42px;height:42px;border-radius:50%;background:#ecfdf5;display:flex;align-items:center;justify-content:center;">
                                <i class="fas fa-book" style="font-size:1.1rem;color:#065f46;"></i>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="card z-card" style="border-left: 4px solid #2563eb;">
                        <div class="card-body py-3 d-flex align-items-center justify-content-between">
                            <div>
                                <small class="text-muted">Active Rules</small>
                                <h3 class="mb-0 font-weight-bold">{{ $activeRules }}</h3>
                            </div>
                            <div style="width:42px;height:42px;border-radius:50%;background:#eff6ff;display:flex;align-items:center;justify-content:center;">
                                <i class="fas fa-check-circle" style="font-size:1.1rem;color:#2563eb;"></i>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            {{-- Table Card --}}
            <div class="card z-card">
                <div class="card-header d-flex align-items-center justify-content-between flex-wrap" style="gap: 0.75rem;">
                    <h3><i class="fas fa-list mr-2" style="color: var(--z-green)"></i>All Rules</h3>
                    <div class="d-flex align-items-center flex-wrap" style="gap: 0.75rem;">
                        @include('partials.village-bank-selector')
                        <select wire:model="categoryFilter" class="z-per-page">
                            <option value="">All Categories</option>
                            @foreach ($categories as $key => $label)
                                <option value="{{ $key }}">{{ $label }}</option>
                            @endforeach
                        </select>
                        <div class="z-search">
                            <i class="fas fa-search si"></i>
                            <input type="text" wire:model.debounce.300ms="search" placeholder="Search rules...">
                        </div>
                        <select wire:model="perPage" class="z-per-page">
                            <option value="10">10</option><option value="15">15</option><option value="25">25</option><option value="50">50</option>
                        </select>
                    </div>
                </div>
                <div class="card-body p-0">
                    <div class="table-responsive">
                        <table class="table z-table mb-0">
                            <thead>
                                <tr>
                                    <th style="width:5%">#</th>
                                    <th style="width:22%">Title</th>
                                    <th style="width:12%">Category</th>
                                    <th style="width:15%">Village Bank</th>
                                    <th style="width:10%">Status</th>
                                    <th style="width:12%">Acknowledged</th>
                                    <th style="width:10%">Created</th>
                                    <th style="width:14%">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @forelse ($rules as $rule)
                                    <tr>
                                        <td>{{ $rule->sort_order }}</td>
                                        <td>
                                            <strong>{{ $rule->title }}</strong>
                                            <br><small class="text-muted">{{ Str::limit($rule->description, 60) }}</small>
                                        </td>
                                        <td>
                                            <span class="badge" style="background:#f1f5f9;color:#475569;font-size:0.78rem;font-weight:600;border-radius:6px;padding:4px 8px;">
                                                {{ $categories[$rule->category] ?? ucfirst($rule->category) }}
                                            </span>
                                        </td>
                                        <td>{{ $rule->villageBank->name ?? '-' }}</td>
                                        <td>
                                            @if ($rule->is_active)
                                                <span class="badge" style="background:#dcfce7;color:#166534;font-size:0.78rem;border-radius:6px;padding:4px 8px;">Active</span>
                                            @else
                                                <span class="badge" style="background:#fee2e2;color:#991b1b;font-size:0.78rem;border-radius:6px;padding:4px 8px;">Inactive</span>
                                            @endif
                                        </td>
                                        <td>
                                            <div class="d-flex align-items-center" style="gap:0.35rem;">
                                                <div style="flex:1;height:6px;background:#e2e8f0;border-radius:3px;overflow:hidden;">
                                                    <div style="width:{{ $rule->acknowledgementRate() }}%;height:100%;background:#16a34a;border-radius:3px;"></div>
                                                </div>
                                                <small class="text-muted">{{ $rule->acknowledgements_count }}</small>
                                            </div>
                                        </td>
                                        <td><small class="text-muted">{{ $rule->created_at->format('d M Y') }}</small></td>
                                        <td>
                                            <div class="d-flex" style="gap:0.35rem;">
                                                <button wire:click="viewDetail({{ $rule->id }})" class="z-action z-action-view" title="View">
                                                    <i class="fas fa-eye"></i>
                                                </button>
                                                <button wire:click="openEdit({{ $rule->id }})" class="z-action" title="Edit" style="background:#eff6ff;color:#2563eb;">
                                                    <i class="fas fa-pencil-alt"></i>
                                                </button>
                                                <button wire:click="toggleActive({{ $rule->id }})" class="z-action" title="Toggle"
                                                    style="background:{{ $rule->is_active ? '#fef3c7' : '#dcfce7' }};color:{{ $rule->is_active ? '#92400e' : '#166534' }};">
                                                    <i class="fas fa-{{ $rule->is_active ? 'ban' : 'check' }}"></i>
                                                </button>
                                                @if (!$rule->isAcknowledgedBy(Auth::id()))
                                                    <button wire:click="acknowledge({{ $rule->id }})" class="z-action" title="Acknowledge" style="background:#ecfdf5;color:#065f46;">
                                                        <i class="fas fa-handshake"></i>
                                                    </button>
                                                @else
                                                    <span class="z-action" style="background:#f0fdf4;color:#16a34a;cursor:default;" title="Acknowledged">
                                                        <i class="fas fa-check-double"></i>
                                                    </span>
                                                @endif
                                                <button wire:click="confirmDelete({{ $rule->id }})" class="z-action z-action-delete" title="Delete">
                                                    <i class="fas fa-trash-alt"></i>
                                                </button>
                                            </div>
                                        </td>
                                    </tr>
                                @empty
                                    <tr>
                                        <td colspan="8" class="text-center py-4 text-muted">
                                            <i class="fas fa-book-open mr-1"></i> No rules found. Create one to get started.
                                        </td>
                                    </tr>
                                @endforelse
                            </tbody>
                        </table>
                    </div>
                </div>
                @if ($rules->hasPages())
                    <div class="card-footer d-flex justify-content-center">
                        {{ $rules->links() }}
                    </div>
                @endif
            </div>

        </div>
    </section>

    {{-- ═══════════════ CREATE / EDIT MODAL ═══════════════ --}}
    @if ($showFormModal)
        <div class="modal fade show d-block" tabindex="-1" style="background:rgba(0,0,0,0.45);">
            <div class="modal-dialog modal-lg">
                <div class="modal-content" style="border-radius:14px;border:none;">
                    <div class="modal-header modal-header-zesco">
                        <h5 class="modal-title"><i class="fas fa-{{ $editingId ? 'edit' : 'plus-circle' }} mr-2"></i>{{ $editingId ? 'Edit Rule' : 'Create Rule' }}</h5>
                        <button wire:click="$set('showFormModal', false)" class="close text-white"><span>&times;</span></button>
                    </div>
                    <form wire:submit.prevent="saveRule">
                        <div class="modal-body">
                            {{-- Village Bank --}}
                            <div class="row mb-3">
                                <div class="col-md-6">
                                    <label class="z-label">Village Bank <span class="text-danger">*</span></label>
                                    <select wire:model.defer="formBankId" class="form-control z-input">
                                        <option value="">-- Select Village Bank --</option>
                                        @foreach ($this->villageBanks as $vb)
                                            <option value="{{ $vb->id }}">{{ $vb->name }} ({{ $vb->code }})</option>
                                        @endforeach
                                    </select>
                                    @error('formBankId') <small class="text-danger">{{ $message }}</small> @enderror
                                </div>
                                <div class="col-md-3">
                                    <label class="z-label">Category</label>
                                    <select wire:model.defer="category" class="form-control z-input">
                                        @foreach ($categories as $key => $label)
                                            <option value="{{ $key }}">{{ $label }}</option>
                                        @endforeach
                                    </select>
                                </div>
                                <div class="col-md-3">
                                    <label class="z-label">Sort Order</label>
                                    <input type="number" wire:model.defer="sortOrder" class="form-control z-input" min="0">
                                </div>
                            </div>

                            {{-- Title --}}
                            <div class="row mb-3">
                                <div class="col-md-12">
                                    <label class="z-label">Rule Title <span class="text-danger">*</span></label>
                                    <input type="text" wire:model.defer="title" class="form-control z-input" placeholder="e.g. Late Payment Penalty">
                                    @error('title') <small class="text-danger">{{ $message }}</small> @enderror
                                </div>
                            </div>

                            {{-- Description --}}
                            <div class="row mb-3">
                                <div class="col-md-12">
                                    <label class="z-label">Description / Full Rule Text <span class="text-danger">*</span></label>
                                    <textarea wire:model.defer="description" class="form-control z-input" rows="5" placeholder="Describe the rule in detail..."></textarea>
                                    @error('description') <small class="text-danger">{{ $message }}</small> @enderror
                                </div>
                            </div>

                            {{-- Active toggle --}}
                            <div class="custom-control custom-switch">
                                <input type="checkbox" wire:model.defer="isActive" class="custom-control-input" id="ruleActiveSwitch">
                                <label class="custom-control-label" for="ruleActiveSwitch">Active</label>
                            </div>
                        </div>
                        <div class="modal-footer">
                            <button type="button" wire:click="$set('showFormModal', false)" class="btn btn-light" style="border-radius:8px;">Cancel</button>
                            <button type="submit" class="btn-zesco">
                                <i class="fas fa-save mr-1"></i> {{ $editingId ? 'Update Rule' : 'Save Rule' }}
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    @endif

    {{-- ═══════════════ VIEW DETAIL MODAL ═══════════════ --}}
    @if ($detailRule)
        <div class="modal fade show d-block" tabindex="-1" style="background:rgba(0,0,0,0.45);">
            <div class="modal-dialog modal-lg">
                <div class="modal-content" style="border-radius:14px;border:none;">
                    <div class="modal-header modal-header-zesco">
                        <h5 class="modal-title"><i class="fas fa-book-open mr-2"></i>{{ $detailRule->title }}</h5>
                        <button wire:click="closeDetail" class="close text-white"><span>&times;</span></button>
                    </div>
                    <div class="modal-body">
                        <div class="d-flex flex-wrap mb-3" style="gap:0.5rem;">
                            <span class="badge" style="background:#f1f5f9;color:#475569;font-size:0.82rem;border-radius:6px;padding:5px 10px;">
                                <i class="fas fa-tag mr-1"></i>{{ $categories[$detailRule->category] ?? ucfirst($detailRule->category) }}
                            </span>
                            <span class="badge" style="background:{{ $detailRule->is_active ? '#dcfce7' : '#fee2e2' }};color:{{ $detailRule->is_active ? '#166534' : '#991b1b' }};font-size:0.82rem;border-radius:6px;padding:5px 10px;">
                                {{ $detailRule->is_active ? 'Active' : 'Inactive' }}
                            </span>
                            <span class="badge" style="background:#eff6ff;color:#1e40af;font-size:0.82rem;border-radius:6px;padding:5px 10px;">
                                <i class="fas fa-university mr-1"></i>{{ $detailRule->villageBank->name }}
                            </span>
                        </div>

                        <div class="card z-card mb-3">
                            <div class="card-body" style="font-size:0.92rem;line-height:1.7;">
                                {!! nl2br(e($detailRule->description)) !!}
                            </div>
                        </div>

                        <p class="text-muted mb-2" style="font-size:0.85rem;">
                            <i class="fas fa-user mr-1"></i> Created by <strong>{{ $detailRule->creator->name ?? 'Unknown' }}</strong>
                            on {{ $detailRule->created_at->format('d M Y H:i') }}
                        </p>

                        {{-- Acknowledgement progress --}}
                        @php $rate = $detailRule->acknowledgementRate(); @endphp
                        <div class="mb-2">
                            <small class="text-muted">Acknowledgement Progress ({{ $rate }}%)</small>
                            <div style="height:8px;background:#e2e8f0;border-radius:4px;overflow:hidden;margin-top:4px;">
                                <div style="width:{{ $rate }}%;height:100%;background:#16a34a;border-radius:4px;transition:width 0.3s;"></div>
                            </div>
                        </div>

                        {{-- Acknowledged by list --}}
                        @if ($detailRule->acknowledgements->count() > 0)
                            <div class="mt-3">
                                <h6 class="font-weight-bold" style="font-size:0.88rem;">
                                    <i class="fas fa-check-double mr-1" style="color:#16a34a;"></i>Acknowledged by ({{ $detailRule->acknowledgements->count() }})
                                </h6>
                                <div class="d-flex flex-wrap" style="gap:0.4rem;">
                                    @foreach ($detailRule->acknowledgements as $ack)
                                        <span class="badge" style="background:#f0fdf4;color:#166534;border-radius:6px;padding:4px 8px;font-size:0.78rem;">
                                            {{ $ack->user->name ?? 'Unknown' }} &middot; {{ $ack->acknowledged_at->format('d M') }}
                                        </span>
                                    @endforeach
                                </div>
                            </div>
                        @endif
                    </div>
                    <div class="modal-footer">
                        @if (!$detailRule->isAcknowledgedBy(Auth::id()))
                            <button wire:click="acknowledge({{ $detailRule->id }})" class="btn-zesco-green">
                                <i class="fas fa-handshake mr-1"></i> Acknowledge
                            </button>
                        @else
                            <span style="color:#16a34a;font-weight:600;font-size:0.88rem;">
                                <i class="fas fa-check-double mr-1"></i> You have acknowledged this rule
                            </span>
                        @endif
                        <button wire:click="closeDetail" class="btn btn-light" style="border-radius:8px;">Close</button>
                    </div>
                </div>
            </div>
        </div>
    @endif

    {{-- ═══════════════ DELETE MODAL ═══════════════ --}}
    @if ($deleteId)
        <div class="modal fade show d-block" tabindex="-1" style="background:rgba(0,0,0,0.45);">
            <div class="modal-dialog">
                <div class="modal-content z-modal" style="border-radius:14px;border:none;">
                    <div class="modal-header" style="background:#fee2e2;border-radius:14px 14px 0 0;">
                        <h5 class="modal-title" style="color:#991b1b;"><i class="fas fa-exclamation-triangle mr-2"></i>Delete Rule</h5>
                        <button wire:click="$set('deleteId', null)" class="close"><span>&times;</span></button>
                    </div>
                    <div class="modal-body">
                        <p>Are you sure you want to delete <strong>{{ $deleteTitle }}</strong>? This will also remove all acknowledgements.</p>
                    </div>
                    <div class="modal-footer">
                        <button wire:click="$set('deleteId', null)" class="btn btn-light" style="border-radius:8px;">Cancel</button>
                        <button wire:click="deleteRule" class="btn btn-danger" style="border-radius:8px;">
                            <i class="fas fa-trash-alt mr-1"></i> Delete
                        </button>
                    </div>
                </div>
            </div>
        </div>
    @endif
</div>
